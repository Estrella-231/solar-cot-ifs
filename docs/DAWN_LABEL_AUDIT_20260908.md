# 四里晨间标签回源审计（2026-09-08）

**结论：原始工作簿中存在连续固定值；已检查窗口的 cohort 聚合完全正确。** 这说明异常来自源观测记录，不能通过修补 15 min 聚合代码解决。固定值的仪器或采集系统成因尚未确证，不能写成已确诊的传感器故障。

## 来源与范围

- 权威登记路径：`/home/Data_Pool_3/chenyi/GHI/data/hunan/raw/stations/四里光伏.xlsx`。
- 本次读取 FD-107 缓存：`/tmp/hunan_m0_staging_20260903/四里光伏.xlsx`。远端缓存和本地临时副本的 SHA-256 均为 `c13c6a066cc7ca3ee0d902bb8a9d6c8ea1cc1618659e86eafa4971bbe9a879f4`，与既有权威工作簿登记值完全一致。本次没有重新哈希慢盘权威路径。
- 工作簿仅含 `结果集` 一个 sheet，802 行（含表头）、291 列；前 3 列为 `名称·`、`OBJ_TYPE`、`DATA_DATE`，其后为 288 个 `VAL0005`～`VAL2400` 字段。
- 本次原始值核验限于训练日期 **2024-07-01 05:30～09:15 BJT**。未提取 test 原始值，未计算 test 指标。
- 证据：[机器可读审计](../audits/dawn_label_source_20260908.json)，保存 46 个原始单元格和 14 个目标窗口重算。

## 原始单元格

异常日期位于 Excel 第 **181 行**，站点名称为 `四里`、`OBJ_TYPE=IRRADIANCE`、`DATA_DATE` 为字符串 `2024-07-01`。`VALHHMM` 是北京时间的 5 min 区间结束字段；值是数字字符串、单元格类型 `s`、格式 `General`，不存在 Excel 浮点日期转换。

| 原始 5 min 时刻（BJT） | 原始 GHI（W/m²） | 单元格证据 |
|---|---:|---|
| 05:30～05:45 | 均为 0 | BQ181～BT181 |
| **05:50～08:45** | **连续 36 个值均为 362** | **BU181～DD181** |
| 08:50 | 204 | DE181 |
| 08:55 | 176 | DF181 |
| 09:00 | 130 | DG181 |
| 09:05 / 09:10 / 09:15 | 159 / 201 / 195 | DH181 / DI181 / DJ181 |

## 聚合重算

按已冻结合同，目标时刻 `t` 的 GHI 是 `t−10 min、t−5 min、t` 三个原始值的算术平均。检查所得 14 个唯一目标对应 224 条不同初始化/lead 的 cohort 行，**全部与源单元格重算一致，最大绝对差为 0 W/m²**。

- 06:00：`(362 + 362 + 362) / 3 = 362`。
- 06:15～08:45：对应三个源值仍全为 362。
- 09:00：`(204 + 176 + 130) / 3 = 170`。
- 09:15：`(159 + 201 + 195) / 3 = 185`。

06:00 的区间晴空均值为 0.7580398059 W/m²，因此原始比例标签为 `kt=477.5474812`。这一个目标在 16 个初始化/lead 上重复出现，占四里训练集 `sum(kt²)` 的约 **93.57%**。它是标签量级诊断，不能当作已训练模型的损失贡献率。极端标签会强烈影响原始 kt MSE；FP16 直接计算其平方还会溢出，损失至少使用 FP32。

## 对后续工作的决定

1. 原始文件、cohort、label、阈值和损失在本次审计中均未改动。不能凭一次局部检查静默删除异常日或截断 kt。
2. 缓存验收、单卡 profile、固定 8 序列工程过拟合可继续；这些不是泛化或 COT 增益证据。
3. 科学 pilot 前需要统一、可复现的源观测 QC 决定。若更改有效标签规则，须对 A/AC 共用 cohort 一致实施、另存新版本并记录影响；不能针对模型表现选择性过滤。
4. 当前结论仅证明所检查窗口的源值连续固定及聚合正确，不能据此宣称整个数据集不存在其他质量问题，也不能诊断固定值产生的具体设备原因。
