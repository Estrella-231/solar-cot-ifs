# 湖南训练期 CPP 缺口补算（2026-09-25）

## 目的与边界

固定湖南 8→16 AGRI manifest 的 train 有 39,589 个唯一帧，原 CPP 只有 12,319 帧（31.1%），缺 27,270 帧；val 的 6,351 帧都有原 CPP。这个缺口不能通过删除 GHI 样本来处理。CPP 仅作为区域 COT 反演器的训练监督或离线参考，未来真实 CPP 不进入业务预报输入。

用户提供的 `FY4B_CPP_V2_Portable.zip` SHA-256 为 `dc7959b7bc7ef467239a261072f8789d416df9e9a7bf19c8d16403e3d113eb7b`，与 FD-107 已部署包逐字节一致，故复用服务器上的包和现有依赖，不再次传输或更换权重。

2025-08-04 的旧全天试跑在 95 个真实 HDF 时次中仅 18 个成功，77 个失败；日志中确认显存不足（batch 512、当时约 24 GiB 可用）是主要失败之一。因此 18 个文件不等于 15 min 生产链验收。

## 当前执行

- 已建立 `scripts/audit_cpp_v2_missing_inputs_20260925.py`，仅从固定 train/val manifest 列出缺原 CPP 的唯一 UTC 时次，并逐一确认原 FY-4B HDF 与当天 ERA5 温度、湿度、地表温度文件。输出与原 CPP 不混写；审计不读取 test。
- 全量输入存在性审计已结束：train 的 27,270 个缺口均有唯一且非空的原 HDF，且三份当天 ERA5 文件齐全；val 无缺口。这只证明文件存在，不证明 HDF 内部各通道、LCCS、推理和最终 QA 均有效。逐帧审计在 FD-107 的 `/home/Data_Pool/zjnu/solar_cot_regional_20260925/cpp_v2_missing_input_audit_v1.json`，SHA-256 `cdf8a03a3f48fbaae6d1b20d1305eb1d592093472f9c8530f8fbab9cd3984353`。
- 已建立 `scripts/run_cpp_v2_missing_pilot_20260925.sh`。首个白天缺口为 `2024-04-02 04:15 UTC`。首跑使用 GPU 0、batch 64、2 个 DataLoader worker；两个 worker 在推理阶段退出，已停止本项目首跑。复跑改用 GPU 5、batch 64、0 个 worker，独立目录 `/home/Data_Pool/zjnu/solar_cot_regional_20260925/cpp_v2_missing_pilot`，不同 run-id。输出策略 `skip`；关闭错误时写空文件。进程启动不代表结果已成功，须核验 `summary.json`、NC 和 QA。
- 已有原 CPP 区域 bank 的 CPU 生产继续在独立目录运行。新 V2 输出不能无标记并入原 bank。
- 单进程复跑已成功写出 `2024-04-02 04:15 UTC` 的 CPP/CLP_P。首帧从启动到写完约 3 分 50 秒；其中整景 HDF/ERA 读取、tile 推理和整景 NC 写盘都发生在共享盘。湖南 256×256 区域坐标误差通过 `1e-4°` 门槛，COT 有效范围占比 100%，区域均值 19.43，QA 上一小时回填占比 0；四里与竺家中心 COT 分别为 28.08 和 24.66。详细见服务器 `cpp_v2_missing_pilot/qc_20240402041500.json`。这是单帧工程验收，不是多季节产品或物理精度验收。
- `2024-04-02 04:00 UTC` 的同刻 V2 重算已完成。湖南区域与历史 CPP 共有有效像元占比 100%，COT 像元 MAE 0.506、V2 减历史均值偏差 +0.168；V2/历史 COT 区域均值分别 18.856/18.687，V2 区域 QA 回填为 0。逐像元核验与产品 SHA 在 `audits/cot_dynamics_20260924/cpp_v2_overlap_qc_20240402040000.json`。这是单时次近似一致性证据，仍须多季节、云型、两站邻域配对检查，不能自动认定生产算法/历史来源完全相同。
- 按首帧约 4 分钟计算，全量 27,270 张整景串行约 75 天（未计失败重试和拥塞）；不能直接据此提交整景全量队列。下一步应先实现并验证只计算湖南区域所需 tile 的生产路径，或在小规模分层样本上证明必要性，再估算多卡预算。

## 放行条件

1. 首帧 CPP/CLP_P 写出成功；UTC 文件名、维度、lat/lon 与湖南 `grid_static` 的逐像元采样一致；COT 有限值和物理范围、云/晴像元、QA 回填比例均记录。
2. 在原 CPP 存在的相同时次另做 V2 复算，比较 COT 分布、局地云边界与站点邻域差异；V2 与原产品不是自动等价的标签来源。不能在没有一致性检验时把两者混成一个监督集。
3. 按审计实际可生产名单分批续跑，分别记录成功、源缺失、运行失败与 QA 回填；只有经 QC 的独立 V2 bank 才可进入 `R_region` 候选训练。正式 GHI 对照仍需相同样本键。
4. 全量生产时间和资源预算以首帧实测吞吐、显存峰值和共享盘读速为依据，不能凭压缩包存在推断 27,270 帧已可快速补齐。
