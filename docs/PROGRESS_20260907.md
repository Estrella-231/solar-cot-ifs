# 第二篇推进记录：2026-09-07

## 晚间推进：S冻结、标签映射和缓存作业

SimVP207380已于约09:56早停：24轮，累计14066步，最佳第4轮val归一化AGRI RMSE0.555505797061281。best与last均CPU重载通过；两节点无残留训练进程。S冻结配置为configs/s_frozen_hunan_seed42.json。

新PBS207522，node21单卡，执行独立val重载（预设BF16容差0.001）、单卡推理profile、32序列特征检查、全部train/val缓存。服务器目录 `/public/home/slfu/ttzhou/swc/irradiance_forecast/SolarCOTIFS_20260907/data/frozen_forecast_trainval_20260907_v1`；日志为同工程forecast_cache.log。未使用test payload或计算test指标。

标签源SHA核验通过，538053条train与99849条validation全部连接到S manifest，无新增排除。产物 `/public/home/slfu/ttzhou/swc/irradiance_forecast/SolarCOTIFS_20260907/data/forecast_label_join_20260907_v1`，audit镜像在audits/forecast_label_join_20260907.json。完整cache完成后仍须逐分片验收、与标签连接、8序列head过拟合，再进入A/AC pilot；不能把特征smoke称为完成端到端GHI门禁。

CPU因果读取witness与独立agent文档复跑通过。恢复校验已绑定分片receipt、全字段shape/finite、统计文件及导入源代码SHA。沿用swc，无环境重建，无对外通知。IFS因果合同缺口仍只阻止AN/ACN。后续各节为本日历史快照。

## 最新变更：SimVP迁移到8卡

按用户最终要求，SimVP已从单卡207360检查点迁移到207380.tc6000，node21/node23各4张A100，每卡batch8、global64。显存约27GiB/卡（训练allocated峰值24.03GiB），已出现真实有限loss训练步。新目录 `/public/home/slfu/ttzhou/swc/irradiance_forecast/HunanSimVPDDP_20260907/runs/simvp16_ddp8_b8_seed42_v1`。source checkpoint继承原湖南模型及Adam，训练/验证/归一化合同不变，DDP/global batch/SyncBN变化单独登记。增加节点本地NVMe帧缓存与数据/计算分段计时；冷缓存阶段仍受共享盘读取影响，暂不将卡数等同加速倍数。旧单卡目录保留。COT冻结状态不变。

## 最新完成：COT R训练

新R作业207372.tc6000已完成，node21/GPU0，PBS墙钟2min。18,514条train/val数据包传输和逐文件哈希校验通过；后台Get-FileHash不可用的问题已修复，旧失败状态已被恢复后的完成状态替代。

单卡profile选batch1024（实测约73,157 samples/s，峰值allocated0.655GiB）；模型仅处理16×16站点块且数据全驻GPU，无需占更多卡。8样本400步eval损失0.19621→0.000288，通过后重新初始化，正式46轮早停，最佳index37。

验证6,536条/1,664,013有效像元：COT MAE2.032317、RMSE4.246485、log1p Huber0.019415。保存预测的独立指标重算一致；6条CPU FP32重载与GPU FP16保存输出最大log1p误差0.002063（预设容差0.01）。test未使用，未评价forecast AGRI域或GHI收益。

冻结资产：`configs/r_frozen_repaired_seed42.json`；最佳权重SHA `0cbdb932f49945259d5f1704316edaf6481be7cfdb54692236f829988925799c`。服务器输出 `SolarCOTIFS_20260907/runs/cot_repaired_seed42_v1`，含checkpoint、norm嵌入metadata、预测、指标、SHA、独立核验。

SimVP207360继续训练，最新核查epoch0/step3220，尚未形成完成选模的冻结资产。下一步仍为S冻结后的forecast cache与32序列端到端/8条过拟合，然后A/AC。下文“当前运行与自动衔接”保留本日早期快照，已由本节替代。

## 已完成

- 修正CPP数据已验收：成功46,036，源缺失84,904，剩余读取错误0；候选21,140。原文件未改写。
- 新R程序按FINAL_PROPOSAL实现：base16两级U-Net、16输入、softplus log1p(COT)、masked Huber(delta0.1)、AdamW、50epoch/patience8。保留同一R的倒数层特征接口供AL/ALN对照。
- 学院CPU测试2项通过；独立agent按文档原样复跑通过，PBS语法正确。GPU门禁未运行。
- M0八项SHA重新核验。972,126条有效白天基础记录逐条通过唯一键、时区、lead、15min标签窗口和原始三条5min均值检查。没有读取未来CPP来筛选。
- 完成区间kt：972,028行，训练538,053、验证99,849、测试334,126。98条区间晴空均值为0，另存排除键与原因。晴空采用pvlib0.13.0/Ineichen、海拔0m假设、区间三个5min中点均值，沿用湖南基线的明确口径。未裁剪kt，未算测试精度。GHI回算误差≤1.14e-13 W/m²。
- IFS缓存索引SHA匹配，23,360条只有step1–4；三份跨年实际源NC也只有4个time，SSRD单位J m**-2。无发布记录，转换历史不作release时间。

## 当前运行与自动衔接

湖南SimVP：PBS207360.tc6000，在学院node21训练。最新本次核验epoch0/step1550，尚未完成第一轮验证。不得根据单步loss作论文结论。

新R：FD-107进程1400030打包train11,978+val6,536，test仅保留引用，不读取payload。输出 `/tmp/cot_repaired_pack_20260907_v1`，日志 `/tmp/cot_pack_20260907_v1.log`。全量数组和provenance在完成后自动传至学院持久目录。

本机后台自动程序状态：`data/cot_launch_20260907/status.json`。流程为COMPLETE→归档三处SHA→目的数组与代码SHA→单次PBS提交→profile→8样本400步过拟合→重新初始化正式训练。提交记录采用锁和意向文件，结果不明时停止。当前尚未提交COT GPU作业，不能称已开始训练。电脑保持运行且SSH可用时程序继续衔接。

学院R工程：`/public/home/slfu/ttzhou/swc/irradiance_forecast/SolarCOTIFS_20260907`。
未来作业号：`submitted_job.json`；训练日志：`train.log`；产物：`runs/cot_repaired_seed42_v1`。

## 权威产物与下一阶段

FD-107工程根目录 `/home/Data_Pool_3/wangyc/irradiance_paper/solar_cot_ifs_20260905`：

- `data/ghi_base_cohort_20260907_v1/{base_cohort.csv,counts.csv,audit.json}`。
- `data/ghi_label_cohort_20260907_v1/{cohort.csv,counts.csv,excluded_keys.csv,audit.json}`；cohort SHA `313153d7279cda3737286bf76000bacb0766e6cc678cef48e925aa5a5c0a1c9b`。
- `audits/ifs_station_source_20260907.json`。

下一阶段需要合规SimVP和R的验证选模结果，冻结后生成forecast AGRI站点缓存并跑32序列端到端/8条过拟合，随后A/AC pilot。当前972,028行是标签基础，尚无forecast缓存或严格因果IFS交集，不能当成四格主表最终样本数。

CPP源NC生成批次到权重的绑定仍未核实，R的训练损失仅针对检索参考。IFS需要同周期更长step和可审计release，不能从现有4步凭空补齐。以上边界留在工程文档；本次未改论文正文或预填收益。
