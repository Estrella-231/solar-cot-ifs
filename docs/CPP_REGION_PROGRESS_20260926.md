# 湖南区域 CPP 实现与核验

## 已完成

湖南 256×256 输出在原 CPP 4051 网格中对应行 `[1213,1469)`、列 `[2061,2317)`，坐标误差为零。计算原始网格的 36 个 128×128 重叠 tile，上下文并集 448×448；不能先裁成 256×256 再重建 tile。

2024-04-02 04:00、04:15 UTC 两个时刻，区域四网和区域 CLP+COT 两网分别与同版本整景 V2 核验：各 65,536 个共同像元，COT MAE/max 均为 0，CLP 和有效掩膜完全一致。完整机器报告在 `audits/cpp_producer/region_20260926/`。这证明两个时刻的实现复现，不是多季节标签质量或 GHI 收益结论。

四网两帧端到端分别 5.894、3.453 秒；两网分别 8.613、4.240 秒。GPU 为共享 A6000，计时受其他进程影响；不能根据两个时刻宣称两网更慢或稳定吞吐。两网峰值 allocated 约 3.12 GiB，不含其他进程。模型和适配器冷启动单独记录在报告，未计入上述单帧数值。

只读取原始 HDF 必需包围框，ERA5 区域结果按小时缓存，LCCS 按年份缓存。为保证 FP32 数值一致，ERA5 每小时仍用原始 4051 尺寸插值算子产生临时数组，再保留区域裁剪；不称所有步骤完全区域化。输出保留 COT、CLP、观测有效掩膜、最终有效掩膜及 QA，禁用历史回填。CPP 是离线教师参考，ERA5 不进入部署输入。

## 已定位的数值问题

batch16 的区域推理曾产生 COT/云相差异；恢复原 batch64 推理形状、尾部填充到64后两帧逐像元复现。直接实现区域双线性插值曾产生约6e-5的辅助输入差异，导致 COT 超过预定容差；现已保留原插值算子。没有放宽 `1e-4+1e-5*abs(reference)` 的验收标准。远程代码目录逐版本冻结，运行中不覆盖源文件。

## 正在执行

已冻结随机种子42的48帧筛查，按四季及北京时间时段代理分层，各层6帧；只使用固定 train/val 清单的 UTC 文件时次，不读取 test，不按模型误差选图。昼夜代理还需太阳几何核验，云型覆盖还需结果检查。24帧整景对照清单已选定，尚未全部生产。

远程根目录：`/home/Data_Pool/zjnu/solar_cot_regional_20260926`，服务器 `FD-107`。

- 冻结代码：`code_v6/`；批量启动脚本 `run_hunan_cpp_region_pilot_v6.sh`。
- 输入选择：`p2_selection_v1/times48.json`、`references24.json`、`sample_identity.json`。
- 48帧结果：`p2_stratified48_two_v6/`；日志 `p2_stratified48_v6.log`。
- 四网见证：`p2_region_inputs_four_v4/`；两网见证：`p2_region_inputs_two_v5/`。

批量脚本支持固定时间JSON和断点续跑；复用前比较代码/权重/grid/时间清单身份及逐文件哈希，拒绝未登记或不一致文件。当前逐帧原子写出，尚未完成正式分片打包和1000帧吞吐验收。

## 后续放行顺序

先完成48帧覆盖与24帧同版本整景对照，再做1000帧连续生产及分片/失败台账。通过后才补产27,270缺口并构建统一V2 train/val bank，随后接入 R_region。现在没有启动全量补产，也没有启动新的 R_region 训练。旧历史CPP区域bank曾遇到NetCDF HDF错误，不能当作已完成资产；与本次V2生产分开审计和恢复。

## 48帧完成与24帧对照启动

48/48 已正常完成。单帧总耗时合计461.08秒，中位9.31秒、nearest-rank p95 15.22秒、最大20.08秒；冷启动11.57秒。运行约7分53秒，没有traceback。分散时刻通常不能共享ERA小时缓存，不等于连续生产速度。

47帧有效覆盖≥99%；`2024-09-16T16:15:00 UTC` 有效54.9484%。独立读取原始HDF并逐通道定标复核：C11–C15有效比例分别61.39%、68.74%、54.95%、58.76%、63.06%；SAZ全有效。五通道共同有效掩膜与保存的observation_valid完全一致，观测有效但模型无效像元为0，观测缺失却填出有限COT像元为0。缺测来自原始卫星通道，仍保留为缺测，不删GHI样本；同版本整景核验尚待该帧完成。

24帧对照已启动：`code_gate_v1/run_cpp_region_fullscene_gate_v1.sh` 调用未修改的原整景CLI，输出 `p2_fullscene24_gate_v1/`，外层日志 `p2_fullscene24_gate_v1.log`，原流水线日志在其 `logs/`。每帧生成整景后核验COT/CLP/mask；任一超限立即停止，不放行1000帧。driver已启动、首帧原卫星和ERA输入已读取；这不是24帧已通过的声明。

1000帧选择按四季各250帧，使用固定train/val UTC清单。冬季不存在完整250帧连续段，因此改为多个真实15分钟连续段拼成各季预算，显式记录段间缺口，不伪造连续时间轴。生产尚未启动，需先通过24帧一致性。

## 24帧通过与1000帧启动

24/24整景对照全部完成并通过。所有帧COT MAE/max_abs均为0，CLP与有限像元掩膜均相同；低覆盖帧共同像元36,011、参考覆盖54.9484%，同样完全一致。报告保存于 `audits/cpp_producer/region_20260926/fullscene24_report.json`；结论仅为该24帧同版本数值复现，不证明物理真值或GHI提升。

已启动1000帧测试，复用冻结的 `code_v6`，CLP+COT、batch64/pad64和区域读取合同不变。启动前GPU5总48GiB、其他进程占约27.2GiB，区域实测峰值约3.12GiB，保留余量；没有扩卡或终止其他任务。不是PBS作业。

- 启动器：`run_hunan_cpp_region_continuous1000_v1.sh`。
- 输出：`p2_continuous1000_two_v6/`；日志：`p2_continuous1000_v1.log`。
- 时间清单：`p2_selection1000_v2/times1000.json`；四季各250帧，段边界保留。

完成后统计缓存复用下median/p95、端到端耗时、有效像元、坏源/坏输出及实际压缩存储量，再验证分片完整性和断点复用。全量补产、统一train/val bank及R_region训练尚未启动。
