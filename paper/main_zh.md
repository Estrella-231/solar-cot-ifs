# 融合 IFS 的 FY-4B 辐照度短临预报：云光学厚度表示的作用

## 摘要
本研究考察在固定卫星外推骨干下，显式云光学厚度（COT）表示能否改善站点辐照度短临预报，以及它是否与IFS条件互补。研究设计分离表示、监督、容量和输入因素。实验结论尚待建立。

## 1 引言
核心问题是在多光谱预报固定时显式云光学表示的价值。同一图像导出的COT被视为归纳偏置，而非独立观测。

## 2 相关工作
重点比较基于COT的辐照度短临和卫星—数值天气预报耦合。提出新颖性主张前需完成最近邻的详细对照。

## 3 数据与预报任务
研究涉及湖南四里和竺家。八张历史帧预测十六张未来帧，时间间隔15分钟。令t为最后一张历史帧时间，$\Delta=15$分钟。对于第k个时效，
$$
\tau_k=t+k\Delta,\quad \mathcal I_{t,k}=(t+(k-1)\Delta,t+k\Delta],\quad k=1,\ldots,16.
$$
区间平均目标及其回算为
$$
\bar k_{s,k}=\frac{\bar G_{s,k}}{\bar G^{cs}_{s,k}},\qquad
\widehat G_{s,k}=\widehat k_{s,k}\bar G^{cs}_{s,k}.
$$
地面观测仅作监督与评价目标。未来观测不进入推理。IFS产品必须在初始化时刻之前或当时已经发布。

## 4 固定骨干下的COT表示
冻结SimVP将历史图像映射为预测AGRI场。冻结反演模型将预测场和确定性太阳几何映射为COT。对数变换COT的四个统计量输入小型站点头。匹配对照采用同一个反演模型、同一CPP监督产生的四维隐特征。IFS提供与目标区间对齐的归一化辐射条件。站点头预测非负晴空指数，不施加上界为1的限制。

## 5 实验协议
主对比为二乘二设计：仅AGRI、AGRI加COT、AGRI加IFS、AGRI同时加两者。各组使用相同队列和固定上游预测。容量和监督对照用于检验替代解释。按日期块配对重采样量化不确定性。对于匹配的均方误差，交互量为
$$
I=\operatorname{MSE}(A)-\operatorname{MSE}(AC)-\operatorname{MSE}(AN)+\operatorname{MSE}(ACN).
$$
负交互量表示该尺度上超过相加的误差降低；该解释必须得到不确定性证据支持。

## 6 结果
选模和协议冻结后，结果将由已保存的匹配预测填入。不预设性能提升。

## 7 讨论
分析将区分显式表示与检索监督的作用，并检查外推误差传播。结论仅限于已评价的站点、时段和云条件。

## 8 结论
结论依据完成后的受控实验给出。
