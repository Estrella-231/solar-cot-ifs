# 第二篇论文工程规则

## 2026-09-10 来源核查与后续授权

用户已授权来源核验后推进空间表示最小对照。最新证据见docs/SOURCE_FOLLOWUP_20260910.md：FD-107登录恢复，CPP生产目录仍700、writer绑定未闭合；新IFS根chensr/ifs_hres_china/raw_nc含2024–2026目录，2025抽样header有100时刻，历史下载请求包含湖南长步长，但时间坐标读取超时、历史发布合同未放行。空间表示准备见docs/COT_SPATIAL_REPRESENTATION_NEXT_20260910.md，可复用已有cot_log1p场，不重建或重跑S/R。本轮未提交训练、未打开test，监控保持关闭。

## 2026-09-10 按需核查：208030完成，种子稳定性未通过

服务器日志出现GHI_MULTISEED_TRAINING_AND_AUDIT_COMPLETE；audits/head_ghi_multiseed_20260910.json为PASS_ALL_12_FIXED_LR_PREDICTIONS_AND_CPU_RELOADS。8新增训练与12固定LR候选同99849验证行/真值/NumPy指标/CPU重载均验收通过，test_used=false。qstat记录已清除，Unknown Job Id不是失败，不重复提交；未取得PBS最终exit_status。

AC−A的GHI RMSE（W/m²，负值有利COT）：original seed42/43/44为−1.010415/−1.080577/+1.090858，均值−0.333378，样本标准差1.233923，2/3改善；QC1为−0.506901/+2.702890/+0.740733，均值+0.978907，样本标准差1.618096，1/3改善。仅新增43/44：original平均+0.005140，QC1平均+1.721812。原始三seed平均A149.959893、AC149.626515；QC1平均A149.132355、AC150.111262。

按冻结协议，新增种子方向不一致，当前四维COT统计拼接配置未通过种子稳定性判据。不能延续seed42的0.67%/0.34%作为稳定提升；不能由此推导COT普遍无效。分站亦不稳定，QC1四里三个种子均退化。保持所有候选与负结果，不自动加seed、调参、改变表示、打开test或恢复小时监控。若用户后续授权研究新表示，须另立单因素和参数匹配对照协议。

## 2026-09-10 最新授权：多种子验证

PBS208030已提交（单A100，提交前账户空队列），复用seed42、新增43/44，original/QC1各A/AC，共8新增训练；固定学习率不逐seed选取。协议docs/HEAD_GHI_MULTISEED_PROTOCOL_20260910.md，执行与验收入口docs/HEAD_GHI_MULTISEED_RUN_20260910.md。单seed208020已完成；本轮完成须以全部训练及12候选独立验收为准。用户未要求持续监控，小时任务继续关闭；无自动后续扩展或test。

## 2026-09-10 用户授权GHI损失对照

用户明确要求按推荐推进。独立协议见docs/HEAD_GHI_LOSS_PROTOCOL_20260910.md，执行记录见docs/HEAD_GHI_LOSS_RUN_20260910.md。PBS208020已提交并确认R/1 A100，原始与QC1各A/AC两档LR共8次小头训练，唯一改变损失，完成状态以训练及独立验收为准。旧配置关闭仍保留为历史结论，不阻止本次已授权的独立损失诊断。小时监控保持关闭，不自动扩展后续实验。

- 2026-09-10最新用户要求：不持续监控。cot每小时自动检查已关闭；208011接续诊断已完成，见docs/HEAD_LOSS_MASS_RESULTS_20260910.md。停止自动轮询和自动追加任务，后续由用户按需触发；旧文档的hourly ACTIVE和Gold等待恢复表述仅为历史。

- 用户于2026-09-08新增限制：服务器同时最多3个未结束PBS任务，按账户跨项目合并统计，保守包含运行/排队/挂起等非完成状态。每次qsub前实时查全队列；达到3个等待，串行提交并逐次重查，不终止别人任务腾位置。此限制与最多8卡同时适用，不能把4个模型串行前向误算成4个PBS任务。

开始数据、训练、评价或论文工作前，必须阅读本仓库 `docs/UPSTREAM_PROJECT_RULES.md` 和 `IRRADIANCE_ERROR_PREVENTION.md`。这两份文件保存建仓时上层项目的数据与评价规则，独立克隆不依赖父目录。范围按用户 2026-09-05 新指令独立定义：湖南 +15～+240 min、固定 SimVP 确定性外推、重点研究 COT 与 IFS 对站点辐照度精度的作用；上游第一篇的 diffusion 方法方向不覆盖本篇范围。

- 本工程根目录为本仓库克隆目录（原始工作区为 `F:/CODE_Classify/辐照度预报/solar-cot-ifs`）；ARIS 全部输出写在本仓库，禁止覆盖第一篇 `solar-energy` 或上层 `refine-logs`。
- 第一篇 diffusion 不作为本篇等待条件，不引入 diffusion 或改造 SimVP 的实验。
- 优先复用经过湖南训练区域、split、通道、归一化和权重哈希审计的 SimVP；不能用旧湖北权重冒充湖南基线。未找到合规权重时，登记前置缺口，必要的湖南基线训练只执行一次。
- COT 是云光学厚度，CPP 是检索监督参考。未来真实 CPP/AGRI 仅限标签或单列 oracle 诊断，不得进入可部署主表。
- COT 由 AGRI 导出，不宣称提供独立观测；主张必须分开表示、监督、参数量及 IFS 的收益。
- IFS 发布时刻与单位审计不通过，不运行完整融合主表；可推进 A/AC。
- 英文 LaTeX 和中文 Markdown 同步更新，章节、公式和结论一致；无结果时保留待完成状态，禁止预写提升数字。
- 单卡先 profile、提高 batch/吞吐并保留显存余量，资源饱和后再扩卡；不得终止第一篇已有作业腾卡。
- 不自动发送飞书、邮件或论文投稿。本次为立项与计划交付，实验执行状态必须如实登记。

## 2026-09-05 继续推进授权
用户已明确要求继续推进，可在已有范围内执行资产审计、代码实现、通过门禁后的smoke/pilot与必要基线准备，无需重复请求许可。当前P200发现旧权重合同不合规，不能以继续推进为由绕过湖南独立训练、时间划分或IFS因果规则。

## 2026-09-08 当前配置研究状态

湖南冻结 S/R、四维 COT 统计和当前小头的 P202 探索矩阵及负收益诊断已完成。original/QC1 均无总体 COT 改善，固定目标 real/forecast 参考误差诊断也未发现具体实现错误；独立 Result-to-Claim 判定为 no，same-family/provisional。按原计划关闭**这一配置**的正向扩展，不重复训练或打开 test。这不是全项目禁用 COT，也不改变既有数据完整性规则。后续以 docs/COT_PAIR_RESULT_TO_CLAIM_20260908.md 与 docs/HOURLY_NEXT_ACTIONS_20260908.md 为准；IFS 等待具体新来源，状态无变化时小时任务安静。

## 2026-09-08 用户最新要求：重开空间审计

用户提出 COT 区域等可能错误并要求修改 hourly、立即检查。当前优先级为主动执行 docs/COT_SPATIAL_REAUDIT_PLAN_20260908.md 的实际像元与源生成地理绑定审计；不能因为先前 P202 closed 而只做静默空队列巡检。明确错误才另名修复、验收并公平复跑，未查明前不覆盖旧数据或重训。当前状态与产物统一见小时交接；其他科学/数据约束保留。

本轮固定12行的CPP/AGRI实际像元链及4片原HDF DN/LUT见证、独立复核已通过；不重复已完成抽查。尚未找到实际CPP writer与批次，不能排除生产者地理/时间错配。下一动作只处理具体生成脚本/日志新线索，见 docs/COT_SPATIAL_REAUDIT_RESULTS_20260908.md 与小时交接；无新线索时保持安静，不凭怀疑重建或训练。

## 2026-09-08 后续用户要求：继续查负收益原因

用户明确要求检查加入云物理属性后变差的原因，允许对已完成候选做有界只读分解、冻结模型只前向与独立复算；不局限于等待producer路径。新证据/受阻状态以 docs/COT_FAILURE_CAUSES_20260908.md 和小时交接为准。新的loss/表示对照须单独冻结合同，不能为预设COT有效而篡改结果或扩大搜索。
